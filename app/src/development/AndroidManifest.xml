<?xml version="1.0" encoding="utf-8"?>
<!--
  Aplica-se APENAS ao flavor development. Aponta o app pro network security
  config que libera cleartext só para 10.0.2.2 (o backend local visto de
  dentro do emulador) — staging/production nunca mesclam este arquivo, e
  continuam exigindo HTTPS via usesCleartextTraffic="false" do manifesto
  principal.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
